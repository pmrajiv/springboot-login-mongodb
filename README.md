Creating a springboot application with basic login using MONGODB using spring security.

First Setup up MongoDB

Create a DB(admin) and collection(login)

Create System user(name:admin password:admin) to access DB and collections

Download STS, Clone the project and import the project in sts and build the project using JDK-17

Ensure the properties file are according to the user db collection created in mongoDB
